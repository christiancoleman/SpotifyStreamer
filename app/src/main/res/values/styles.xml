<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColor">@color/spotify_font_color</item>
        <item name="android:textColorHint">@color/spotify_font_color</item>
        <item name="android:editTextColor">@color/spotify_font_color</item>
    </style>

</resources>
